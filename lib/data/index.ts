import { crmDeals, demoSuppliers } from "@/data/crm";
import { insights, monthlyKpis, demoBatches } from "@/data/kpis";
import { knowledgeAnswers } from "@/data/knowledge";
import { catalogProducts } from "@/data/products";
import { qualityControls } from "@/data/quality";
import { readinessRows } from "@/data/readiness";
import { bottlenecks, workflowNodes } from "@/data/workflow";

export const catalog = {
  catalogProducts,
  workflowNodes,
  bottlenecks,
  monthlyKpis,
  insights,
  demoBatches,
  demoSuppliers,
  crmDeals,
  knowledgeAnswers,
  qualityControls,
  readinessRows,
};
